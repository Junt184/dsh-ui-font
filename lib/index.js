// Host half of dsh-ui-font. Client-only: font preferences live in the
// browser's localStorage and the client overrides the font tokens directly.
// This no-op apply only satisfies the dual-face roster-row loading contract.
export function apply(_ctx) {}
